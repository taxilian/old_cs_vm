include <iostream>
include "LexicalParser.h"

int main(int argc, char *argv[] )
{
    LexicalParser parser("lexer\\test.kxi");
    
    Token cur;
    do {
        cur = parser.nextToken();
        std::cout << cur.toString() << std::endl;
    } while (cur.type != Token::TT_ENDOFFILE);

    int a = 28534;
    a += 37812;

    char c = '\n';

    return 0;
}